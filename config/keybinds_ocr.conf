# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                    OCR Keybindings ONLY                     ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

# NormCap - Direct OCR (no screenshot first!)
bind = $mainMod SHIFT, V, exec, normcap

# Alternative: OCR with parse mode (URLs, emails, etc)
bind = $mainMod SHIFT, C, exec, normcap --mode parse
